1) dynamic part: ZTP
-Collect Data into a csv/excel spreadsheet 
(excel will require import xlrd (sudo pip install xlrd)
(csv will require import csv)
	a) os image
	b) base config per switch
		i) hostname
		ii) me0 IP address
		iii) root password
		iv) services: ssh, syslog server, snmp?,...
		v) static default route
	c) add device to management platform - like rancid (http://www.shrubbery.net/rancid/)
	d) Time stamp each step in the log
2) SPACE Network Director / Home grown code / ansible other
	a) pulls config from cvs/git/vcs
		i) pushes to box via netconf
		ii) commit check / commit
	b) connectivity check / verification step
	c) 
